interface:
  display_name: "Jamovi Analysis"
  short_description: "Help with jamovi-native stats workflows and saved .omv projects"
  default_prompt: "Run jamovi-native statistical workflows with the local jamovi installation, preferring project mode with a JobFile containing a unified JSON contract. Always use the preprocess stage for raw survey data."
